'use client';
import { FormEvent, useState } from 'react';

export default function Contact(){const [sent,setSent]=useState(false);function submit(e:FormEvent){e.preventDefault();setSent(true)}return <main className="section section-light" style={{minHeight:'100vh'}}><div className="container" style={{maxWidth:900}}><div className="eyebrow">Request a pilot</div><h1 style={{fontSize:58,letterSpacing:'-.04em',margin:'12px 0 15px'}}>Bring us one operational workflow.</h1><p className="muted" style={{fontSize:18}}>We start with a concrete process — reporting, incidents, QHSE or industrial data — then build a measurable pilot.</p>{sent?<div className="card" style={{marginTop:35}}><h2>Request received.</h2><p>We will use the information you submitted to define the next technical step.</p></div>:<form onSubmit={submit} style={{display:'grid',gap:14,marginTop:35}}>{['Name','Company','Role','Work email','Plant / location','Main operational challenge','Current tools (Excel, ERP, GMAO, etc.)'].map((x,i)=>i===5?<textarea key={x} required placeholder={x} rows={5} style={{padding:16,border:'1px solid #c8cbc7',borderRadius:10,background:'#fff'}}/>:<input key={x} required={i<4} placeholder={x} style={{padding:16,border:'1px solid #c8cbc7',borderRadius:10,background:'#fff'}}/>)}<button className="button button-green" type="submit" style={{justifySelf:'start'}}>SUBMIT PILOT REQUEST</button></form>}</div></main>}