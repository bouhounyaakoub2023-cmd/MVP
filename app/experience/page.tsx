import { PageFrame } from '../../components/site-shell';
import Link from 'next/link';
const work=[['DEMONSTRATION','Industrial operations intelligence lab','Synthetic factory environment used to demonstrate production, maintenance, QHSE and data workflows.','Demo'],['ENGINEERING','AI automation workflows','AI extraction, structured data, workflow orchestration and operational notifications built as reusable engineering patterns.','Explore'],['COLLABORATION','From prototype to pilot','We start with a concrete workflow, measure it, then decide whether it should become a reusable product or remain a tailored system.','Talk to us']];
export default function Experience(){return <PageFrame><main className="section-light"><section className="container page-hero"><div className="eyebrow">Experience / evidence</div><h1>Show the system.<br/><span>Don't sell the promise.</span></h1><p>Our public work distinguishes demonstrations, engineering experiments, pilots and client work. We use the same standard internally: if it isn't built or measured, we don't present it as a result.</p></section><section className="container experience-list">{work.map(([k,t,d,c],i)=><article key={t}><div className="experience-no">0{i+1}</div><div><div className="eyebrow">{k}</div><h2>{t}</h2><p>{d}</p></div><Link href={c==='Demo'?'/demo':'/contact'} className="text-link">{c} ↗</Link></article>)}</section></main></PageFrame>}
