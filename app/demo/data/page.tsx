import Link from 'next/link';

const stages=[['01','SOURCE','Excel / ERP / GMAO / CSV / Forms / Documents'],['02','INGEST','Webhook or scheduled import'],['03','VALIDATE','Schema, types, required fields, anomalies'],['04','NORMALIZE','Map sources into one industrial model'],['05','INTELLIGENCE','Rules + AI extraction + correlation'],['06','ACTION','KPI / report / alert / follow-up']];
export default function DataDemo(){return <main className="section section-light" style={{minHeight:'100vh'}}><div className="container" style={{maxWidth:1100}}><Link href="/demo" className="eyebrow">← Demo Lab</Link><h1 style={{fontSize:48,margin:'18px 0 8px'}}>INDUSTRIAL DATA FLOW</h1><p className="muted" style={{maxWidth:760}}>The product does not ask a factory to replace its existing systems. It creates a controlled intelligence layer around the operational data already being produced.</p><section className="card dark-card" style={{marginTop:35}}><div className="eyebrow">REFERENCE ARCHITECTURE</div>{stages.map(([n,t,d])=><div key={n} style={{display:'grid',gridTemplateColumns:'60px 180px 1fr',gap:15,alignItems:'center',padding:'18px 0',borderBottom:'1px solid rgba(255,255,255,.12)'}}><span style={{fontWeight:800}}>{n}</span><strong>{t}</strong><span className="muted">{d}</span></div>)}</section><div className="grid" style={{marginTop:18}}><article className="card"><div className="eyebrow">DESIGN RULE</div><h2>AI never becomes the source of truth.</h2><p>Structured operational records remain the system of record. AI adds extraction, interpretation and recommendations with confidence and review states.</p></article><article className="card"><div className="eyebrow">SAFETY BOUNDARY</div><h2>Intelligence before control.</h2><p>The first product versions produce insight, reports, alerts and human-reviewed actions. They do not directly control PLCs or safety-critical equipment.</p></article></div></div></main>}
