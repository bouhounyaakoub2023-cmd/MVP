import Link from 'next/link';
import { PageFrame } from '../../../components/site-shell';

const stages=[['01','SOURCE','Excel / ERP / GMAO / CSV / Forms / Documents'],['02','INGEST','Webhook or scheduled import'],['03','VALIDATE','Schema, types, required fields, anomalies'],['04','NORMALIZE','Map sources into one industrial model'],['05','INTELLIGENCE','Rules + AI extraction + correlation'],['06','ACTION','KPI / report / alert / follow-up']];
export default function DataDemo(){return <PageFrame><main className="demo-subpage section-light"><div className="container"><div className="demo-subnav"><Link href="/demo">← Demo Lab</Link><span>Industrial data flow</span></div><div className="subpage-heading"><div className="eyebrow">04 / Data architecture</div><h1>The path from<br/><span>source to action.</span></h1><p>The platform does not ask a factory to replace its existing systems. It creates a controlled intelligence layer around the operational data already being produced.</p></div><section className="card dark-card data-flow-card"><div className="eyebrow">REFERENCE ARCHITECTURE</div>{stages.map(([n,t,d],i)=><div className="flow-stage" key={n}><span>{n}</span><strong>{t}</strong><p>{d}</p>{i<stages.length-1&&<b>↓</b>}</div>)}</section><div className="grid data-rules" style={{marginTop:18}}><article className="card"><div className="eyebrow">Design rule</div><h2>AI never becomes the source of truth.</h2><p>Structured operational records remain the system of record. AI adds extraction, interpretation and recommendations with confidence and review states.</p></article><article className="card"><div className="eyebrow">Safety boundary</div><h2>Intelligence before control.</h2><p>The first product versions produce insight, reports, alerts and human-reviewed actions. They do not directly control PLCs or safety-critical equipment.</p></article></div><div className="subpage-next"><Link href="/demo">Return to Demo Lab →</Link></div></div></main></PageFrame>}
